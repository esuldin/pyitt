name: Build Python package
description: Build Python package for a module.

author: Egor Suldin

runs:
  using: composite
  steps:
    - name: Build Python package
      run: |
        python -m build
      shell: ${{runner.os == 'Windows' && 'cmd' || 'sh'}}
